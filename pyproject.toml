[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "video-upscaler"
version = "1.0.0"
description = "High-Performance Hardware-Accelerated Video Upscaler with C++ Vulkan/AVX-512 backend"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24.0",
    "click>=8.0.0",
    "rich>=12.0.0",
    "pillow>=9.0.0",
    "tqdm>=4.60.0",
    "opencv-python>=4.5.0",
]

[project.scripts]
video-upscaler = "video_upscaler.cli:main"
video-upscaler-gui = "video_upscaler.gui:launch_gui"

[tool.setuptools.packages.find]
where = ["."]
include = ["video_upscaler*"]
